name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.27.0'
        channel: 'stable'
    
    - name: Install dependencies
      run: |
        echo "Checking available SDK versions..."
        flutter pub get || {
          echo "Error: Failed to install dependencies. This might be due to SDK version not being published yet."
          echo "Current SDK version in pubspec.yaml:"
          grep "nimbbl_mobile_kit_flutter_webview_sdk" pubspec.yaml || true
          exit 1
        }
    
    - name: Analyze code
      run: flutter analyze
      continue-on-error: true
    
    - name: Build web
      run: |
        flutter build web --release --web-renderer canvaskit --base-href /nimbbl_flutter_sample_app/
    
    - name: Copy 404.html for SPA routing
      run: |
        if [ -f web/404.html ]; then
          cp web/404.html build/web/404.html
        fi
    
    - name: Preserve CNAME if exists
      run: |
        if [ -f web/CNAME ]; then
          cp web/CNAME build/web/CNAME
        fi
    
    - name: Setup Pages
      uses: actions/configure-pages@v4
    
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: build/web
    
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
